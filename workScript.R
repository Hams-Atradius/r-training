a <- 1

f <- function(x) {
  return (x^2)
}

f(1)

round(1.5)
round(2.500001)


l <- list(name="Jane", age=3, grades = c(4,56,6,3))

str(l) 

df <- data.frame(a=c(1,2,3), b = c("A", "B", "C")) ; df

str(df)

str(mtcars)

View(iris)


kml <- mtcars$mpg * 0.425144

mtcars$kml <- kml

mtcars



